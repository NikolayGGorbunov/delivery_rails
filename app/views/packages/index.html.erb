<div class="container">
  <h2>
      <%= sortable "price", "Price" %>
      <%= sortable "distance", "Distance" %>
      <%= sortable "updated_at", "Date" %>
      <br><br>
  <% @packages.each do |pack| %>
    <%= link_to "Package №#{pack[:id]} info", pack %>
    <br>
  <% end %>
  </h2>
  <%= paginate @packages %>
</div>
